let map = fn f =>
        { fn list =>
        { match list {
                | nil -> nil
                | v :: tail -> cons(f(v), map(f)(tail))
        }
}};
let myList = cons(1, cons(2, cons(3, cons(4, cons(5, nil)))));
map (fn x => { x * 2 })(myList);;
