let fibo = fn a, b => { lcons(a, fibo(b)(a + b)) };
let lv = box(fibo(0)(1));
let count = box(10);
while (*count ~= 0) {
        match *lv {
                | nil -> println("never happens")
                | v :: tail -> println(v); lv := tail
};
count := *count - 1
};;
